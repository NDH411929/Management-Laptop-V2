extends ../../layouts/default.pug
include ../../mixins/alert.pug

block main
    +alert-success(100000)
    +alert-error(100000)
    .forgot-password(style="padding:16px 0;background:#ececec")
        .container.my-5
            .inner-content(style="background:#fff")
                .row-left(style="margin:0 auto;width:45%;padding:100px 24px")
                    h1.title(style="text-align:center") Nhập mã xác thực
                    p.desc(style="text-align:center;margin-bottom:75px") Mã xác thực đã được gửi tới email của bạn <b>#{email}</b>. Vui lòng kiểm tra hòm thư!
                    form(
                        action="/user/password/otp"
                        method="POST"
                    )
                        div(class="form-group d-none")
                            label(for="email") Email
                            input(
                                type="email"
                                class="form-control"
                                id="email"
                                name="email"
                                required
                                value=email
                                readonly
                            )
                        div(class="form-group" style="margin-top:25px")
                            input(
                                type="text"
                                class="input-email"
                                id="otp"
                                name="otp"
                                placeholder="Nhập mã xác thực"
                                required
                            )
                        div(class="form-group")
                            button(
                                type="submit"
                                class="btn btn-primary btn-block btn-send"
                                style="background:#e30019;border-color:#e30019"
                            ) Xác minh tài khoản
                    .resend(style="text-align:center") Không nhận được mã? <a href="#!">Gửi lại</a>